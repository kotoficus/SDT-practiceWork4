# Общее описание решения
Проект состоит из файлов, написанных на языке программирования Python и содержащих функции для вычисления площадей и периметров некоторых фигур (круга, квадрата).
# Обзор функций
## Файл circle.py
### Функция *area(r)*
```
def area(r):
    return math.pi * r * r
```
Принимает на вход радиус круга **r**, вычисляет (по форуле `S = πr^2`) и возвращает его площадь **S**.
- **Пример:**
|Входные данные|Выходные данные|
|--------|--------|
|0|0.0|
|1|3.141592653589793|
|2|12.566370614359172|
### Функция *perimeter(r)*
```
def perimeter(r):
    return 2 * math.pi * r
```
Принимает на вход радиус круга **r**, вычисляет (по форуле `P = 2πr`) и возвращает его периметр **P**.
- **Пример:**
|Входные данные|Выходные данные|
|--------|--------|
|0|0.0|
|1|6.283185307179586|
|2|12.566370614359172|
## Файл square.py
### Функция *area(a)*
```
def area(a):
    return a ** 2
```
Принимает на вход сторону квадрата **a**, вычисляет (по форуле `S = a^2`) и возвращает его площадь **S**.
- **Пример:**
|Входные данные|Выходные данные|
|--------|--------|
|0|0|
|1|1|
|2|4|
### Функция *perimeter(a)*
```
def perimeter(a):
    return 4 * a
```
Принимает на вход сторону квадрата **a**, вычисляет (по форуле `P = 4a`) и возвращает его периметр **P**.
- **Пример:**
|Входные данные|Выходные данные|
|--------|--------|
|0|0|
|1|4|
|2|8|
## История коммитов
commit d078c8d9ee6155f3cb0e577d28d337b791de28e2 (HEAD -> main, origin/main, origin/HEAD)

Author: smartiqa <info@smartiqa.ru>

Date:   Thu Mar 4 14:55:29 2021 +0300

    L-03: Docs added

commit 8ba9aeb3cea847b63a91ac378a2a6db758682460

Author: smartiqa <info@smartiqa.ru>

Date:   Thu Mar 4 14:54:08 2021 +0300

    L-03: Circle and Square added
# Описание Unit тестов
##Файл "test_circle.py" содержит класс, описывающий 6 групп тестов, нацеленных на проверку корректности работы функций area(r) и perimeter(r).
### Обзор функций
1. Функции test_area_normal_values и test_perimeter_normal_values проверяют корректность работы функций area(r) и perimeter(r) соответственно, передавая в качестве аргументов небольшие положительные числа.
2. Функции test_area_zero_value и test_perimeter_zero_value проверяют корректность работы функций area(r) и perimeter(r) соответственно, передавая в качестве аргумента 0.
3. Функции test_area_big_values и test_perimeter_big_values проверяют корректность работы функций area(r) и perimeter(r) соответственно, передавая в качестве аргументов положительные числа, большие, чем аргументы функций пункта 1.
##Файл "test_square.py" содержит класс, описывающий 6 групп тестов, нацеленных на проверку корректности работы функций area(a) и perimeter(a).
### Обзор функций
1. Функции test_area_normal_values и test_perimeter_normal_values проверяют корректность работы функций area(a) и perimeter(a) соответственно, передавая в качестве аргументов небольшие положительные числа.
2. Функции test_area_zero_value и test_perimeter_zero_value проверяют корректность работы функций area(a) и perimeter(a) соответственно, передавая в качестве аргумента 0.
3. Функции test_area_big_values и test_perimeter_big_values проверяют корректность работы функций area(a) и perimeter(a) соответственно, передавая в качестве аргументов положительные числа, большие, чем аргументы функций пункта 1.